using UnityEngine;

namespace ScringloGames.ColorClash.Runtime.UI
{
    public class ConditionHUDIndicator : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
        
        }

        // Update is called once per frame
        private void Update()
        {
        
        }
    }
}
